<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude local ML cache and user-sensitive records from device backups. -->
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
</full-backup-content>
